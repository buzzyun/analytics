<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fastcatgroup.analytics.db.mapper.RelateKeywordMapper">

	<update id="createTable" parameterType="map">
		create table RelateKeyword (
			id int not null auto_increment,
			site varchar(64),
			category varchar(64),
			keyword varchar(64),
			updateTime timestamp,
			primary key (id)
		);
	</update>
	
	<update id="createIndex" parameterType="map">
		create index category_index on RelateKeyword(site, category)
	</update>
	
	<select id="validateTable" parameterType="map" resultType="map">
		select id, site, category, keyword, updateTime
		from RelateKeyword
		limit 1
	</select>

	<update id="dropTable" parameterType="map">
		drop table RelateKeyword
	</update>
	
	<select id="getEntry" parameterType="map" resultType="org.fastcatgroup.analytics.db.vo.RelateKeywordVO">
		select * 
		from RelateKeyword where category = #{category} and keyword = #{keyword}
	</select>
	<!-- where a.category=#{category} -->
	<select id="getEntryListByWhereCondition" parameterType="map" resultType="org.fastcatgroup.analytics.db.vo.RelateKeywordVO">
		select a.id, a.site, a.category, a.keyword, 
		concat(group_concat(b.value)) value, 
		a.updateTime
		from RelateKeyword a left join RelateKeywordValue b on b.keyId = a.id
		<if test="whereCondition != ''">
		${whereCondition}
		</if>
		group by a.id
		limit #{start}, #{end}
	</select>
	<select id="getCountByWhereCondition" parameterType="map" resultType="int">
		select count(*) from RelateKeyword where category=#{category} 
		<if test="whereCondition != ''">
		${whereCondition}
		</if>
	</select>
	<select id="getEntryList" parameterType="map" resultType="org.fastcatgroup.analytics.db.vo.RelateKeywordVO">
		select * from RelateKeyword where category=#{category}
	</select>
	
	<select id="getCount" parameterType="map" resultType="int">
		select count(*) from RelateKeyword where category=#{category}
	</select>
	
	<insert id="putEntry" parameterType="map" useGeneratedKeys="true" keyProperty="id">
		insert into RelateKeyword 
		(category,keyword,updateTime) 
		select * from (select #{category},#{keyword},#{updateTime} ) a
		where (select count(*) from RelateKeyword where category=#{category} and keyword=#{keyword} ) = 0
	</insert>
	
	<update id="updateEntry" parameterType="map">
		update RelateKeyword set 
		category = #{category},
		keyword = #{keyword},
		value = #{value},
		updateTime = #{updateTime},
		where id = #{id}
	</update>
	
	<delete id="deleteEntry" parameterType="int">
		delete from RelateKeyword where id=#{id}
	</delete>
	
	<delete id="deleteEntryList" parameterType="map">
		delete from RelateKeyword
		where id in (${idList})
	</delete>
	
	<delete id="truncate" parameterType="map">
		truncate table RelateKeyword
	</delete>
	
</mapper>
