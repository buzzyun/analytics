<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.fastcatgroup.analytics.db.mapper.SearchPathKeywordHitMapper">

	<update id="createTable" parameterType="map" >
		create table ${siteId}_searchpath_hit_keyword (
			timeId varchar(11),
			keyword varchar(128),
			searchType varchar(30),
			hit int,
			primary key (timeId, keyword, searchType)
		) ENGINE=InnoDB, CHARACTER SET=utf8 COLLATE utf8_unicode_ci;
	</update>
	
	<update id="createIndex" parameterType="map" >
		create index timeId_${siteId}_searchpath_hit_keyword on ${siteId}_searchpath_hit_keyword(timeId, keyword, searchType)
	</update>
	
	<select id="validateTable" parameterType="map" resultType="map">
		select timeId, keyword, searchType, hit
		from ${siteId}_searchpath_hit_keyword
		where categoryId = ''
	</select>

	<update id="dropTable" parameterType="map">
		drop table ${siteId}_searchpath_hit_keyword
	</update>

	<delete id="truncate" parameterType="map">
		truncate table ${siteId}_searchpath_hit_keyword 
	</delete>
		
	<select id="getEntryList" parameterType="map" resultType="org.fastcatgroup.analytics.db.vo.SearchPathKeywordHitVO">
		select * from ${siteId}_searchpath_hit_keyword 
		<where>
			timeId=#{timeId}
			and keyword=#{keyword}
		</where>
	</select>
	
	<delete id="updateClear" parameterType="map">
		delete from ${siteId}_searchpath_hit_keyword
		where 
		<where>
			timeId=#{timeId}
			and keyword=#{keyword}
		</where>
	</delete>
	
	<insert id="putEntry" parameterType="map" useGeneratedKeys="true" >
		insert ignore into ${siteId}_searchpath_hit_keyword 
		(timeId, keyword, searchType, hit) values (#{timeId}, #{keyword}, #{searchType}, #{hit})
	</insert>


</mapper>