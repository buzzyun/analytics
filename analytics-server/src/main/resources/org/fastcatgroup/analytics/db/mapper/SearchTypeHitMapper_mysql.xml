<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.fastcatgroup.analytics.db.mapper.SearchTypeHitMapper">

	<update id="createTable" parameterType="map" >
		create table ${siteId}_type_hit_${typeId} (
			categoryId varchar(64),
			timeId varchar(11),
			dtype varchar(32),
			hit int,
			primary key (categoryId, timeId, dtype)
		) ENGINE=InnoDB;
	</update>
	
	<update id="createIndex" parameterType="map" >
		create index dtype_${siteId}_type_hit_${typeId} on ${siteId}_type_hit_${typeId}(dtype)
	</update>
	
	<select id="validateTable" parameterType="map" resultType="map">
		select categoryId, timeId, dtype, hit
		from ${siteId}_type_hit_${typeId}
		limit 1
	</select>

	<update id="dropTable" parameterType="map">
		drop table ${siteId}_type_hit_${typeId}
	</update>

	<delete id="truncate" parameterType="map">
		truncate table ${siteId}_type_hit_${typeId} 
	</delete>
	
	<select id="getEntry" parameterType="map" resultType="org.fastcatgroup.analytics.db.vo.SearchTypeHitVO">
		select * from ${siteId}_type_hit_${typeId} 
		where categoryId = #{categoryId}
		and timeId=#{timeId}
		and dtype=#{dtype}
	</select>
	
	<select id="getEntryList" parameterType="map" resultType="org.fastcatgroup.analytics.db.vo.SearchTypeHitVO">
		select * from ${siteId}_type_hit_${typeId} 
		where categoryId = #{categoryId}
		and timeId=#{timeId}
	</select>
	
	<insert id="putEntry" parameterType="map" useGeneratedKeys="true" >
		insert into ${siteId}_type_hit_${typeId} 
		(categoryId, timeId, dtype, hit) 
		values 
		(#{categoryId}, #{timeId}, #{dtype}, #{hit})
	</insert>
	
	<update id="updateEntry" parameterType="map">
		update ${siteId}_type_hit_${typeId} 
		set hit = #{hit} 
		where categoryId = #{categoryId}
		and timeId=#{timeId}
		and dtype=#{dtype}
	</update>
	
</mapper>